import { PruebaNombrePipe } from './prueba-nombre.pipe';

describe('PruebaNombrePipe', () => {
  it('create an instance', () => {
    const pipe = new PruebaNombrePipe();
    expect(pipe).toBeTruthy();
  });
});
